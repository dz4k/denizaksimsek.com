<%_ 
    const fmt = it.format ??
        it.iso ? "ATOM"
        : it.lang === "tok"
            ? "'󱥜' yy '󱤺' MM '󱥤' dd" + (it.time ? " 󱥫 HH:mm" : "")
            : "dd MMM \u2018yy" + (it.time ? " HH:mm" : "") _%>
<%_= filters.date(date, fmt, lang) _%>
