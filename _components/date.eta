<%_ 
    const fmt = format ??
        iso ? "ATOM"
        : lang === "tok"
            ? "'󱥜' yy '󱤺' MM '󱥤' dd" + (time ? " 󱥫 HH:mm" : "")
            : "dd MMM \u2018yy" + (time ? " HH:mm" : "") _%>
<%_= filters.date(date, fmt, lang) _%>
